server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: C:\\Users\\akubany\\dev\\configuration\\dev\\config-repo
        # Use the File System Backend to avoid git pulling. Enable "native" profile in the Config Server.
        native:
          searchLocations: file:///C:/Users/akubany/dev/configuration/dev/config-repo
  profiles:
    active: native
  application:
    name: aos-config
logging:
  level:
    root: DEBUG